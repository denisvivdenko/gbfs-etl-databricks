resources:
  pipelines:
    gbfs_etl:
      name: gbfs_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      continuous: false
      libraries:
        - glob:
            include: ../pipelines/bronze/**
      environment:
        dependencies:
          - --editable ${workspace.file_path}
