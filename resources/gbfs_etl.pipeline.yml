resources:
  pipelines:
    gbfs_etl:
      name: gbfs_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      continuous: false
      root_path: "../src/pipelines/gbfs_etl"
      libraries:
        - glob:
            include: ../src/pipelines/gbfs_etl/transformations/**
